# Demo_Ant_Design

// Thêm xóa sửa (Chức năng)
// Sửa lại CSS table
// Tạo phần có thể login logout thay đổi tài khoản
// Nhiều dữ liệu phải làm gì
// chuyen dang mobile
//css login
//Xử lý dữ liệu dài trong một ô

//???
const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

Cac kiểu dữ liệu trong typescript và chúng xài như thế nào: 
interface User {
id: number;
name: string;
address: string;
}

Câu trả lời: boolean, number, String, null undifine

Usestate là gì
Use...???

null undifine khác nhau ntn?

"data và setdata khác nhau ntn"?
const [data, setData] = useState<User[]>(generateUsers(300));
Tìm hiểu khái niệm render và rerender?
Khi nào sử dụng render, khi nào dùng rerender 
Học và áp dụng React Hooks

Định nghĩa API, Tạo 1 trang hiển thị table, hiển thị theo kiểu card